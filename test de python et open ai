test pour tester python dans VS code : 
from flask import Flask

app = Flask(__name__)

@app.route('/')
def hello():
    return "<h1>Test de Python réussi !</h1><p>Le serveur Flask tourne correctement.</p>"

if __name__ == '__main__':
    app.run(debug=True)
test de l integration du openai : 
import os
from groq import Groq
from dotenv import load_dotenv

load_dotenv()

client = Groq(api_key=os.getenv("cle api"))

response = client.chat.completions.create(
    model="llama-3.3-70b-versatile",
    messages=[
        {"role": "system", "content": "Tu es un assistant cuisine."},
        {"role": "user", "content": "Donne moi une recette de crêpes"}
    ]
)

print(response.choices[0].message.content)
